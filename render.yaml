services:
  - type: web
    name: wealthfam
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        value: duckdb:///family_finance_v3.duckdb
      - key: VITE_APP_BUILD
        value: render-free

